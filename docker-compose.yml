version: '3.8'

services:
  titanic-app:
    build: .
    ports:
      - "5000:5000"  # Bind directly to avoid confusion
    container_name: titanic_prediction_container
    restart: unless-stopped  # Safer than always
